pipeline {
  agent {
    kubernetes {
      yamlFile 'docker/pod.yaml'
    }
  }
  stages {
    stage('Docker build') {
        sh "ls -la"
      steps {
        container('docker') {
          sh 'docker run hello-world'
        }
      }
    }
  }
}