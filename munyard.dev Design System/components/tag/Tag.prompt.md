Category/topic pill in mono type — post categories, filter chips.

```jsx
<Tag color="mint">virtualisation</Tag>
<Tag active onClick={...}>linux</Tag>
```

`color` neutral|mint|amber|info; `active` fills solid mint. Lowercase labels by convention.
